[package]
name = "shell-cli"
version = "0.1.0"
edition = "2021"
description = "Fast helper CLI for the shells Quickshell config"
license = "MIT"

[[bin]]
name = "shell"
path = "src/main.rs"

[profile.release]
codegen-units = 1
lto = "thin"
opt-level = "z"
panic = "abort"
strip = true
